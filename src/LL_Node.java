public class LL_Node <T>{
    private Object<T> data;
    private LL_Node<T> next;
    private LL_Node<T> last;

    // Ctor
    public LL_Node(Object data, LL_Node<T> next, LL_Node<T> last) {
        this.data = data;
        this.next = next;
        this.last = last;
    }

    // Need to implement
    public Remove_Node(int numPages, String nameOfCreator) {
    }

    // Implement
    public Add_After(LL_Node next){

    }
}
